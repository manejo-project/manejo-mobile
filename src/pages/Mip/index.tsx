import React from 'react';
import FormMip from '../../components/mip/formMip';

import { Container, Title } from './styles';

const Mip: React.FC = () => {
  return (
    <Container>
      <FormMip />
    </Container>
  );
};

export default Mip;
